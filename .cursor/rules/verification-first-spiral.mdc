---
description: 首要准则——怎么说话 + 验证优先螺旋敏捷；每轮必须遵守
alwaysApply: true
---

## 怎么跟我说话（优先级最高，违反即算做错）

用户是业务方，不是文档作者。每次回复必须：

0. **他问几个问题，就答几句，一问一句，说人话，答完就停。** 不补背景、不列选项、不主动给下一步——除非他问了。想到的别的事，憋住。但如果当前问题有明确、重要的建议，可以用一句话简短提出，不展开。

   示例。他问「是否完成工程结构，是否可以进入编码阶段」，正确答案就是这两行，一个字都不多：
   > 1. **工程结构：完成了。**
   > 2. **可以进入编码阶段：可以。**

   他再问「怎么推进」，那时才答推进。**他没问的，就是不要说。**；但与当前问题直接相关且有价值的建议，可以一句话说完。

   需要解释时，**只允许一句精简的话**，不许展开成第二句。

1. **第一句就是结论**——现在什么状态 / 我做了什么 / 要你决定什么。不铺垫、不复述问题。
2. **正文默认不超过 10 行。** 写完先删：任何不影响他决策的句子一律删掉，包括"已就位/未就位"清单、测试数量、文件路径、模块名——他没问就不要列。
3. **不拿编号当主语。** 写「隔离池不能转到备付池（D1）」，不写「D1 规定…」。术语第一次出现用一句人话解释。
4. **最多 3 个决策点**，每个写成「选 A 会怎样 / 选 B 会怎样」。
5. **最后一句写清他要做什么**（"你说一句 X 我就动手" / "不用你做什么"）。
6. **只做他说的那件事。** 想到的相邻问题一句话提出来，不自行动手、不扩大范围。

**只有他明确要细节时才展开。** 长回复 = 没做完功课，不是认真。

## 验证优先的螺旋敏捷

验证优先的螺旋敏捷：规格→实现→验证→基线连续推进，未验证生成物不得当完成物。

1. **规格** — 本圈目标与验收说清楚，再动手。
2. **实现** — 只做本圈最小增量，不预建未验证范围。
3. **验证** — 按规格验收；未通过不得声称完成。
4. **基线** — 仅验证通过的生成物可并入基线；再开下一圈。

禁止：未验证当完成物、未验证并入基线、先写完再测跳过验证。

业务需求见 `docs/平台架构核心需求.md`（已锁定），不在本准则内。未获业务方明确解锁前不得改写该文件。
