# 控制器 Client 验收矩阵（C0）

> 权威以 `docs/specs/控制器Client.md` 为准。本圈只书面判定，不连真设备、不写 App。  
> 编号 C0.* 供规格验收；实现圈另用应用服务 / 本机袋夹具落地，不得放宽本表。

| 编号 | 条件 | 允许 / 拒绝 | 归属 | 审计 |
| --- | --- | --- | --- | --- |
| C0.1 | 包名沿用 `com.gbndt.shijiaoqi` 覆盖 6.1 | 允许；现场只这一份 App | 同一应用身份 | 无 |
| C0.2 | 覆盖后未导入的 `/sdcard/ShiJiaoQi` 工程/工艺 | 拒绝读取、拒绝激活、拒绝按路径焊接 | 旧文件不当本机袋 | 拒绝痕迹；无正文 |
| C0.3 | 系统已发 Client UUID，登记时读到设备识别号，本厂绑定有效且该号未绑他机 | 允许；该 Client 钉死该设备号 | 设备号属该 Client | `client_register` 允许 |
| C0.4 | 注册时读不到设备识别号 | 拒绝 | 绑定与本机袋不变 | `client_register` 拒绝 |
| C0.5 | 读到的设备号已钉在本厂另一台未作废 Client | 拒绝 | 两台原件不变 | `client_register` 拒绝 |
| C0.6 | 用设备号或旧 MAC 注册码当作平台 Client UUID | 拒绝 | 身份不变 | 拒绝 |
| C0.7 | 换臂：重新注册读到新设备号 | 允许；旧号作废，新号钉上 | 仍是同一 Client 身份 | `client_register` 允许 |
| C0.8 | 未登录领取解封钥或解开闭包正文 | 拒绝 | 磁盘信封不变 | 拒绝 |
| C0.8a | 超管改 Client 策略（范围/钥是否落盘/时效/上限） | 允许；对本厂全部 Client | 本厂一行意图 | `set_client_policy` 允许 |
| C0.8b | `cache_scope=all` 登录成功 | 允许缓存该人获准工程+工艺密文（受上限） | 本机袋信封 | 无正文 |
| C0.8c | `cache_scope=current` 离线且无当前工程信封 | 拒绝激活 | 袋不变 | 拒绝 |
| C0.8d | `persist_unwrap_key=false` 杀进程未再登录 | 拒绝解明文 | 信封仍在 | 拒绝 |
| C0.9 | 退出或换人，本机工程/工艺都没有「改过」 | 允许；清本机缓存与内存钥 | 袋空 | 无正文入审计 |
| C0.9a | 有「改过」时退出未确认 | 拒绝退出 | 本机缓存与标记不变 | 无正文 |
| C0.9b | 有「改过」，确认后退出或换人 | 允许；清本机缓存、内存钥与明文 | 袋空 | 无正文入审计 |
| C0.10 | 工程正文非空 `processId` 不在本行 `deps` | 拒绝写入；原件不变 | 修订不升 | `update_asset` 拒绝 |
| C0.11 | 空 `processId` | 允许；不进 `deps` | 按写入口 | 允许 |
| C0.12 | 按 T 排模版添加一条焊缝 | 允许；字段是 T 排自己的（含 `gapBands`），`templateId` 为该份 | 新元素 | `create_asset` / 改正文 |
| C0.13 | 单层 `templateId` 的元素带了 `gapBands` 或坡口点 | 允许写入（改正文不套模版）；激活/焊接不当 T 排 | 原元素仍是单层 | 无 |
| C0.14 | T 排 `gapBands[].rootProcessId` / `capProcessId` 非空但不在 `deps` | 拒绝 | 原件不变 | `update_asset` 拒绝 |
| C0.15 | 工程正文再写 `processPath` 或内嵌工艺参数 | 拒绝 | 原件不变 | 拒绝 |
| C0.16 | 厂端导入：工艺文件先入库再映射工程路径 | 允许；厂级资产；`processId` ⊆ `deps` | 新厂级工艺/工程 | `import_legacy` 允许 |
| C0.17 | 导入时某条 `processPath` 找不到对应已入工艺 | 拒绝整份工程；已入工艺保留 | 该工程不存在 | `import_legacy` 拒绝 |
| C0.18 | 现场 App 扫描本地文件树做导入 | 拒绝 | 本机袋不变 | 拒绝 |
| C0.19 | WAN 自动按文件名映射身份 | 拒绝（云端无文件树） | 平台级不变 | 拒绝 |
| C0.20 | 导入后的标准工艺仍以本机 `Standard/` 或 CDN zip 为准 | 拒绝 | 闭包成员才是真相 | 拒绝 |
| C0.21 | Client 持游离全量工艺库 | 拒绝 | 只持闭包成员 | 拒绝 |
| C0.22 | 只拷闭包文件、没有本机身份与设备号绑定 | 拒绝激活 | 拷来的不当本机袋有效缓存 | 拒绝 |
| C0.23 | 焊接中切换激活或撤该缓存 | 拒绝 | 当前闭包不变 | 拒绝 |
| C0.24 | 他厂账号在本厂已绑定控制器登录 | 拒绝 | 会话不建立 | 拒绝 |
| C0.25 | 不可复制平台级已下发到本机 | 允许焊接用内存明文；列表可名称/状态/修订/编号 | 仍不可复制 | 无正文给人看、无正文进审计 |
| C0.26 | 空库工程模版 | 允许三份：单层、多层、T排对接；无包角 | 各份独立修订 | 种子写入 |
| C0.27 | 已有库未手建 T 排模版 | 允许维持已有份；不自动改已有工程正文 | 旧模版不变 | 无 `apply_template` |
| C0.28 | 在 App 改正文或新建平台级 | 拒绝 | 厂库与本机袋平台级不变 | 拒绝 |
| C0.29 | 操作员在 App 改厂级原件 | 拒绝；厂级保持 `clean`。可复制则可另存个人级 `created` | 厂库厂级不变 | 拒绝原件 |
| C0.30 | 管理员改过厂级后连上；Web 已另改过（厂端版本号更高） | 允许；App 正文覆盖厂库当前行并升修订 | 厂库以 App 为准；其他 Client 旧快照不变 | 允许；无正文 |
| C0.31 | 创建人离线改自己的个人级后连上；Web 已另改过 | 允许；App 正文覆盖并升修订 | 厂库以 App 为准 | 允许；无正文 |
| C0.32 | 非创建人在 App 改别人的个人级 | 拒绝 | 原件不变 | 拒绝 |
| C0.33 | 无权角色保存厂级或平台级 | 拒绝；状态不得变成 `edited` | 厂库不变 | 拒绝 |
| C0.34 | `clean`、厂端清单版本号一样 | 允许；不动 | 两边不变 | 无 |
| C0.35 | `clean`、厂端清单版本号更高 | 允许；拉厂端；仍 `clean` | 本机跟上厂端 | 无正文 |
| C0.36 | `edited`、厂端清单版本号一样或更高 | 允许；App 覆盖并升修订；成功后 `clean` | 厂库以 App 为准 | 允许；无正文 |
| C0.37 | 清单没有且本机 `edited`（厂级或个人级） | 允许；用原身份重建；成功 `clean` | 厂库重新有这一条 | 允许；无正文 |
| C0.37a | 清单没有且本机 `clean` 或 `deleted` | 允许；本机行去掉；不重建 | 厂库不变 | 无 |
| C0.37b | 厂端停用、本机 `edited` 后连上 | 允许盖正文；厂端保持停用；App 列表不显示 | 停用仍在 | 允许；无正文 |
| C0.37c | App 删除个人级；厂端无引用 | 允许；厂端删除；成功后本机行消失 | 厂库无此条 | 允许 |
| C0.37d | App 删除个人级；厂端因仍被引用拒绝 | 拒绝删除；本机撤销 `deleted` 并拉齐为 `clean` | 厂库原件仍在 | 拒绝 |
| C0.37e | App 删除厂级或平台级 | 拒绝 | 厂库不变 | 拒绝 |
| C0.37f | 管理员新建厂级或个人级 | 允许；身份为 UUIDv7；成功后本机行仍在且 `clean` | 厂库有同一身份 | 允许；无正文 |
