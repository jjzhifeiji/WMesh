services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: wmesh
      POSTGRES_PASSWORD: wmesh
    ports:
      - "55433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wmesh"]
      interval: 2s
      timeout: 5s
      retries: 20
